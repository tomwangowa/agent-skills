# Control return

When returning control, add a three-option next-step block only if all three
options are concrete, useful, and within the user's scope. Mark one
**建議** option and give one short reason. Do not manufacture options merely to
fill the list. Omit the block for a simple answer, a clarification question,
an in-progress update, or when no genuine next decision exists.
